<!-- generated by `teams learn` — fingerprint: f39572655bf71f83 — 2026-08-18 20:24 — model: claude-sonnet-5 — do not edit; put your own rules in frontend.local.md -->
STACK: TypeScript React 19 + Vite 8 + Tailwind v4 + react-router-dom 7, no backend/db (frontend only, talks to sample-backend via /api proxy), package manager pnpm
COMMANDS:
- run: pnpm dev | test: unknown | lint: pnpm lint (oxlint) | build: pnpm build (tsc -b && vite build) | format: unknown

STRUCTURE:
- src/lib/api.ts: API client for sample-backend
- Pages: `/` dashboard (stats), `/projects` (list/create/delete), `/projects/:id` (tasks CRUD + filter), `/about`
- unknown (no docs describing components/routes file layout beyond above)

CONVENTIONS:
- Dev server runs on port 5173, proxies `/api` to backend at localhost:8010 (per vite.config.ts; README states :8000, vite.config.ts overrides to :8010 — trust vite.config.ts)
- Tailwind v4 via @tailwindcss/vite plugin (no separate tailwind.config needed for v4)
- Build is type-checked first (tsc -b) then bundled (vite build) — code must pass TypeScript compilation before build succeeds
- Linting via oxlint, not eslint

NEVER:
- unknown (no explicit prohibitions found in docs/config)

SEE:
- README.md / readme.md (duplicate, identical content) for page/route overview
- src/lib/api.ts for API client patterns before adding data fetching
- vite.config.ts for proxy port (authoritative over README)
