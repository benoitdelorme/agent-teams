<!-- generated by `teams learn` — fingerprint: f39572655bf71f83 — 2026-08-18 20:21 — model: claude-sonnet-5 — do not edit; put your own rules in frontend.local.md -->
STACK: TypeScript/React 19 + Vite + Tailwind CSS v4 + react-router-dom, no backend db (talks to sample-backend via /api proxy), package manager pnpm
COMMANDS:
- run: pnpm dev | test: unknown | lint: pnpm lint (oxlint) | build: pnpm build (tsc -b && vite build) | format: unknown
STRUCTURE:
- src/lib/api.ts: API client for backend calls
- Pages: `/` dashboard, `/projects` list/create/delete, `/projects/:id` tasks CRUD + filter, `/about`
- unknown exact directory layout beyond README-described pages/API client
CONVENTIONS:
- Dev proxy: `/api` → backend at localhost:8010 (vite.config.ts), use relative `/api/...` paths, not absolute backend URLs
- Tailwind v4 via `@tailwindcss/vite` plugin (no separate tailwind.config referenced) — use utility classes
- react-router-dom for routing
- TypeScript project uses split configs (tsconfig.app.json / tsconfig.node.json) via references
- unknown state management pattern beyond React built-ins (no state lib in deps)
NEVER:
- unknown (no explicit prohibitions found in docs/config)
SEE:
- README.md / readme.md (duplicate, same content) — pages list and dev commands
- src/lib/api.ts — before making any API calls
- vite.config.ts — proxy port/config before touching backend integration
