<!-- generated by `teams learn` — fingerprint: 041cf50ceaa015d9 — 2026-08-18 20:22 — model: claude-sonnet-5 — do not edit; put your own rules in backend.local.md -->
STACK: Python 3.11+ / FastAPI + SQLite / no frontend styling / uv package manager
COMMANDS:
- run: uv run uvicorn app.main:app --reload --port 8010 | test: uv run pytest | lint: unknown | build: unknown | format: unknown
STRUCTURE: unknown
CONVENTIONS:
- REST API structure: /api/health, /api/stats, /api/projects[/{id}], /api/projects/{id}/tasks, /api/tasks/{id} (PATCH/DELETE)
- Uses Pydantic (>=2.7) for data models/validation
- Uses PyJWT (>=2.8) — implies JWT-based auth somewhere in app
- DB file (data.db) is auto-created and auto-seeded on first start
NEVER: unknown
SEE: app/main.py (entrypoint), pyproject.toml (deps), no CLAUDE.md/AGENTS.md present
